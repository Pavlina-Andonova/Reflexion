<section id="events">
    <div class="container">
        <div class="heading">
            <h1 class="title text-center">{{title}}</h1>
        </div>
        {{#each events}}
            {{> home/single-event this}}
        {{/each}}
    </div>
    <div class="clearfix"></div>
</section>
